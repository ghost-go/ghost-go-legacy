extend type Query {
	levelFilter: String!
	tagFilter: String!
	ranges: [String]
	settings: Settings!
  viewer: [Query]
	# kifuFilter: String!
	# dateRangeFilter: String!
	# userRangeFilter: String!
	# recordTypeFilter: String!
  # nextStoneType: Int
  # toolbarHidden: Boolean!
  # boardStates: BoardStates!
  # ranges: [String]
  # theme: String!
}

type Settings {
  isFilterMenuOpen: Boolean!
}

